# Use the platform charset for Gradle launcher argument files. This keeps
# forked test JVM classpaths valid when the checkout path contains non-ASCII
# characters on Windows. Java source and resource encodings remain UTF-8.
org.gradle.jvmargs=-Dfile.encoding=COMPAT -Xmx1g
